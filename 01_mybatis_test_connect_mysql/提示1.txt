0、现在是没有使用注解来写SQL   也是只写了dao层、没有service层和controller层

1、我把xml映射文件放在实体类一起，测试的时候说找不到，然后放在resources下,再起个和dao接口相同的目录才行
2、运行测试的时候会生成一个target目录，和src目录同级，你clean一下就没有了。
    target是用来存放项目构建后的文件和目录、jar包、war包、编译的class文件

3、SSM项目：
  		测试一：实体类和数据库表某个字段和数据库不对应的话，不会报错；但是那个字段就拿不到值，是null而已。
  		测试二：字段名一样，但是数据库是int类型；java是String类型；还是可以拿到该字段的数据，保存可能就不行。
  		测试三：但是如果是数据库是int类型；java你使用Date类型就会直接报错。
  		测试四：如果是实体类在和数据库对应的基础上实体类再多出几个字段是没有问题的。
