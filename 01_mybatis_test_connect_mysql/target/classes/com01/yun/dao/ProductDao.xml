<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--这个映射配置文件可以直接放在实体类文件夹下，也可以放在resources里面(但是你里面其的目录结构要和实体类的结构一样)-->
<mapper namespace="com01.yun.dao.ProductDao">
    <select id="queryAllProduct" resultType="com01.yun.pojo.Product">
        SELECT * FROM product
    </select>
    <!--
    总结：
    1、namespace就是命名空间;写的是dao接口的全限定类名
    2、id就是接口的方法名
    3、resultType就是返回结果类型 -->
</mapper>

